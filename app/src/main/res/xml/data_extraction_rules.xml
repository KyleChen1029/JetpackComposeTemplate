<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- TODO: Add <include> and <exclude> rules here for what data shouldn't be backed up.
        Use <exclude .../> to exclude specific data from cloud backup.
        Use <include .../> to include specific data in cloud backup.
        Without <include/> rules, all data is backed up.
        Without <exclude/> rules, all data is backed up.
        Also note that the file limit for cloud backup is 5MB. -->
        <include domain="sharedpref" path="."/>
        <exclude domain="sharedpref" path="device.xml"/>
    </cloud-backup>
    <!--
    <device-transfer>
        <include .../>
        <exclude .../>
    </device-transfer>
    -->
</data-extraction-rules>
